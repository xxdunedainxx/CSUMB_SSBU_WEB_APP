# Tests Dashboard placeholders

- Estimated durations on cards are set to “pending” until we have real timings.
- Results analyzer: each paradigm now has its own upload block and tailored metrics (e.g., No-Go error rate, Posner valid/invalid means); unsupported formats will show a parse error.
